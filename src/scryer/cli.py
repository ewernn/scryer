"""scryer CLI entry point.

Noun-first, kebab-case, singular noun pattern (per plan §13). Real commands
land in Phase 1+; this stub keeps `scryer` as a runnable script for testing
the install.
"""

from __future__ import annotations

import sys


def main() -> int:
    print("scryer 0.0.0 — pre-alpha (CLI commands land in Phase 1)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
